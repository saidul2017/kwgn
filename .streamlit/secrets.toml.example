# Optional: Set default Gemini API key untuk semua user.
# Salin file ini ke .streamlit/secrets.toml di Streamlit Cloud (Settings > Secrets)
# atau buat file .streamlit/secrets.toml lokal (jangan commit!).

# GEMINI_API_KEY = "your-api-key-here"
